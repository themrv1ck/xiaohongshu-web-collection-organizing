---
name: xiaohongshu-web-collection-organizing
description: v2.0 小红书收藏与点赞整理。先在用户授权的已登录浏览器中读取列表，再以 OCR、声音、字幕和画面生成分类方案；创建专辑或移动笔记前始终单独确认。支持快速/完整启动、受控分段、验证恢复和每周新增内容整理。
---

# 小红书整理 Skill v2.0

这是给 RedSkill 同步的单文件版本。它不要求 Codex CLI；视频画面能力可以使用本地 MiMo-VL、已有 Agent/API 或任何已验证可接收真实图片帧的视觉模型。

## 不可省略的边界

- 读取、分类和移动分开。分类方案、OCR、字幕、转写和画面 memo 都不等于移动授权。
- 只使用用户在**当前这次整理**明确指定、已登录且已打开目标列表页的浏览器；不自动换浏览器，也不复用过去的浏览器许可。
- 不读取、复制、记录或外传 cookie、token、xsec 或完整签名 URL。
- 出现安全验证、异常访问、登录页、页面绑定丢失或写入状态不确定时，保存进度并停止；不刷新、不重试、不模拟真人、不尝试绕过。

## 欢迎页

向用户显示：

> 欢迎使用小红书整理 Skill。
>
> 该 Skill 由 Mr.Vick 制作
> 作者小红书：549077233
>
> 我可以帮你整理收藏和点赞内容：
>
> - 图文笔记可读取图片里的中英文文字；
> - 视频可按声音、字幕和画面内容分类；
> - 最终移动前会先让你确认分类方案。
>
> 第一步：请选择启动方式
>
> 1. 快速启动（按推荐设置）
> 2. 完整启动（自己逐项设置）

选择后再问：

> 这次想整理哪一部分？
>
> 1. 收藏
> 2. 点赞
> 3. 我全都要

“我全都要”时，收藏与点赞本地按 note id 去重，并保留两个来源。

## 两种模式共同的前半段

### OCR：读取前先问

> **图文 OCR（推荐开启，以提高识别率）**
>
> OCR 是把图片里的中英文文字读出来。它会读取图文笔记封面和全部内页图片，而不只看封面。
>
> 例如标题只写“建议收藏”，内页写着“上海两天一夜路线、餐厅和交通方式”。开启后可以归到“旅行 / 上海攻略”，而不是只凭标题猜。
>
> OCR 只能读图片文字，不能理解没有文字的纯画面。它通常能覆盖大约 60% 的图文整理场景，所以推荐安装；不安装也能继续，只是分类可能不够准确。
>
> 1. 开启 OCR
> 2. 暂不开启

用户开启后先检查已有能力：

```bash
python3 scripts/check_environment.py --capability-preflight
python3 scripts/check_environment.py --ocr
```

- 已有中英文 OCR：直接复用。
- 缺失：先显示当前系统实际安装内容和体积，再征得安装确认。
- macOS：优先系统 Vision，通常无额外 OCR 模型下载；缺少 Command Line Tools 时以 macOS 安装窗口实际大小为准。
- Windows：默认 Tesseract + `chi_sim` + `eng`，通常几十到数百 MB，默认目录常为 `C:\Program Files\Tesseract-OCR\`；这行只显示给 Windows 用户。
- 用户不开启：后续必须显式使用 `--skip-ocr`，不能静默复用 OCR。

### 当前浏览器与 JavaScript

> 请确认已经打开一个已登录小红书的浏览器，并停在本次要整理的收藏或点赞页面。
>
> 此页面需要允许 Skill 运行必要的 JavaScript，用于读取当前列表；不会在未确认前创建专辑或移动笔记。
>
> 1. 使用默认浏览器
> 2. 选择另一个已登录浏览器

只在用户本次确认后检查该浏览器、登录状态、目标页面和 JavaScript 能力。未登录时请用户自行登录；不后台轮询，不创建独立浏览器 profile 代替用户已有会话。

### 一次设置分段读取

> 这次每组读取多少条、每组之间暂停多久？
>
> 默认：每组最多 200 条，暂停 3 分钟。
>
> 你可以回复“默认”，或回复“每组 <数量> 条，暂停 <分钟> 分钟”。每组最多 200 条。

保存本次设置：

```bash
python3 scripts/onboarding_config.py run_config.json \
  --startup-mode <quick|complete> --source <collection|liked|all> \
  --ocr <enabled|disabled> --batch-size <1-200> --pause-minutes <正整数>
```

用户一次确认后，任务执行器可在用户没有说“暂停”时按该设置安排下一组。每组独立落盘。`--capture-mode segmented` 只校验、读取并保存当前组；它不会自行滚动、刷新、打开 URL 或续段。暂停是用户工作节奏，不是规避验证或伪装真人的方式。

页面实际显示总数后才可展示进度，例如“已读取 200 / 1000 条”；没有可靠总数时只能显示已读取数量，不能猜测。

## 读取完成后再决定内容能力

全部读取完后，先显示真实分布：图文 X 条、视频 Y 条、待确认 Z 条。尚未创建专辑，也没有移动笔记。

### 快速启动

- 图文多：推荐使用 OCR（如已开启）加标题、正文、标签和作者。
- 视频多：再显示视频选择。
- 类型待确认多：留给人工复核，不按简介猜进专辑。

视频选择：

1. 耳朵和眼睛都要（通过字幕、视频语音和完整时轴画面识别内容）
2. 先检查我是否已有类似功能的 AI 或模型
3. 不开启

必须给例子：标题“太香了”的视频实际在讲番茄炒蛋，声音/字幕可归到“做饭 / 菜谱”；无声衣柜整理视频则需要真实画面才可靠归到“收纳整理”。

### 完整启动

逐项选择：视频声音/字幕、完整时轴画面、已有模型或 Agent、安装缺失组件、分类规则、已有专辑是否保留。完整启动可单独选择“只分析声音”。

- 声音：平台字幕或 `MiMo-V2.5-ASR-MLX`，约 6.6 GB。
- 画面：`MiMo-VL-7B-RL-2508` + MLX-VLM，约 16.6 GB；建议 32 GB 统一内存的 Apple Silicon。
- `codex-cli` 不是强制。可使用 `mimo-vl-mlx`、已有 Agent/API 的 `command` 适配器，或用户明确的其他视觉能力。
- MiMo-VL 只看画面；声音来自平台字幕或 ASR。
- 缺失组件必须先检查、说明用途和体积，再征得用户安装确认。

## 分类方案预览与移动

先在本地生成 `classification.json`。展示**分类方案预览**：建议专辑名、每类数量、低置信度与待确认数。默认不展开条目，随后问：

> 是否展开查看每个专辑里的条目？
>
> 1. 展开查看
> 2. 只看数量，继续下一步

只有用户确认分类方案、已有专辑策略和目标专辑后，才生成 dry-run。只有再次明确确认后，才能创建专辑或真实移动。真实移动逐条核验，任何安全或状态不确定立即停止。

## 安全验证后的恢复

遇到验证或异常时：保存已完成分段、分类进度和 `xhs_safety_state.json`，停止所有读取、详情、视频和移动。告诉用户：先在小红书完成验证并回到目标列表页，然后回复“继续整理”。

收到“继续整理”后：只检查当前授权页面与保存状态是否一致；不重读已保存分段、不自动重试旧请求、不在恢复检查时继续视频或移动。检查通过后才按原设置处理未完成段。

## 每周整理新内容

第一次完整整理结束后，问：

> 本次整理已完成。
>
> 是否开启每周固定时间的小红书整理任务？
>
> 开启后，Skill 会复用本次的整理范围、每组读取数量与暂停时间、OCR、视频识别、分类设置和审阅方式。
>
> 每周只整理新出现的内容，并先生成新的分类方案；不会自动创建专辑或移动笔记。
>
> 1. 开启每周整理
> 2. 暂不开启

用户开启后询问星期和时间。到点时任务只准备本周新内容；仍需要用户当次确认已登录浏览器。不得沿用旧浏览器授权，不得自动移动。

每周读取后先筛新增条目：

```bash
python3 scripts/weekly_delta.py visible_items.json weekly_delta.json \
  --baseline weekly_processed_note_ids.json
```

默认只生成预览。只有新的 `classification.json` 覆盖全部新增 note id 后，才提交基线：

```bash
python3 scripts/weekly_delta.py visible_items.json weekly_delta.json \
  --baseline weekly_processed_note_ids.json \
  --commit-after-plan classification.json
```

任何每周任务都不自动创建专辑或移动笔记。
