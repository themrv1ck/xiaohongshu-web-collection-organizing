# Changelog

## 2.0.0 — 2026-07-27

- 新增“快速启动（按推荐设置）”和“完整启动（自己逐项设置）”两个入口。
- 启动顺序改为：启动方式 → 整理范围 → OCR → 当前浏览器检查 → 分段设置 → 实际读取 → 按真实分布选择视频能力 → 分类方案预览 → 确认移动。
- OCR 改为两种模式都在读取前询问，支持中英文图片文字；先检查已有能力，缺失才询问安装。
- 视频内容分类改为根据实际读取到的图文/视频分布按需开启；支持声音、字幕和完整时轴画面，且不绑定 Codex CLI。
- 新增本地 `onboarding_config.py`，保存范围、OCR、每组最多 200 条、暂停时间、审阅方式与每周任务设置。
- 新增每周整理配置与 note id 基线：只整理新内容、只生成分类方案、需要当次浏览器确认、绝不自动移动笔记；不完整的分类方案不能推进基线。
- 分类方案预览默认只显示专辑和数量；用户确认后才展开条目、创建专辑或移动笔记。
- 安全验证恢复改为保留已完成进度，用户完成验证并回复“继续整理”后再检查并恢复未完成部分。
